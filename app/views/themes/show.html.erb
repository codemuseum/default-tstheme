<%
  javascripts params[:id] if File.exist?(File.join('public', 'javascripts', "#{params[:id]}.js"))
  stylesheets params[:id]

  alml_file = File.join('public', 'almls', "#{params[:id]}.alml")
  @theme.alml = File.new(alml_file).read if File.exist?(alml_file)
%>